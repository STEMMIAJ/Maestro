# Sessao 2026-04-22 — Maestro

Derivado de: `conversations/raw/perplexity_conversation_2026-04-22_full.md`.

## Contexto rapido (retomar em 30s)

Dr. Jesus (perito medico, TEA+TDAH) discutiu com Perplexity a arquitetura do
ecossistema STEMMIA Dexter. Decidiu criar um projeto de governanca chamado
`openclaw-control-center` (renomeado para `Maestro`, futuro `Maestro`),
dentro de `~/Desktop/STEMMIA Dexter/`. O projeto nao executa pericia; ele
organiza, audita e orquestra o resto.

## O que foi feito/discutido na conversa

- Definida divisao de papeis: Perplexity = arquiteto; Claude Code = executor;
  OpenClaw = supervisao/cron/memoria/dashboard; Dr. Jesus = validador.
- Desenhada estrutura de pastas completa do projeto (AGENTS, RULES, FLOWS,
  CRON, SCRIPTS, CONFIG, DATA, WEB-UI, conversations, memory, logs, reports,
  docs/openclaw-official).
- Listados 8+ agentes com missao, escopo, entradas, saidas, proibicoes,
  criterio de completude.
- Definido pipeline: capturar conversa -> transformar em memoria estruturada
  -> scripts Python reutilizaveis -> enriquecer projeto -> docs oficiais
  OpenClaw -> relatorios (modelos, custo, DB, Telegram, dashboard) ->
  task list mestra -> checkpoint final.
- Formalizado comando-mestre gigante em 8 fases (0–8) para colar no Claude
  Code e executar tudo em uma rodada.
- Definida a filosofia de memoria: `MEMORY.md` estavel + `memory/*.md` diarios
  + `reports/*` derivados + `conversations/raw/*` como fonte.
- Estabelecido: nao publicar, nao instalar, nao configurar cron real, nao
  usar FTP, nao modificar producao do site nesta rodada.
- Discutido uso do proprio `openclaw dashboard` como UI inicial e, depois,
  dashboard proprio em `stemmia.com.br`.
- Discutido banco de dados (Supabase vs alternativas) e integracao Telegram
  (bot Stemmia) como planos, nao execucoes.
- Tambem discutido (bloco inicial da conversa, antes da mudanca de escopo):
  projeto paralelo de banco de dados de laudos periciais judiciais —
  taxonomia, fontes, buscas, prompts para Claude, avaliacao de qualidade,
  laudos elogiados em sentenca. Isso e um segundo eixo do ecossistema, nao
  e o Maestro em si.

## Decisoes importantes desta sessao

1. Criar pasta `openclaw-control-center` (hoje `Maestro`) como centro
   de governanca.
2. Nao migrar — integrar por papeis.
3. Comecar pela fundacao (estrutura + arquivos + agentes + regras) antes de
   qualquer execucao real de cron ou integracao externa.
4. Preservar conversas em `.md` brutos e gerar derivados curados.
5. Pipeline de ingestao de conversas mora em pasta Python base dentro de
   `STEMMIA Dexter` (subprojeto `conversation_ingestion/`).
6. Nome final do projeto sera `Maestro`; paths sem acento/espaco/ç.

## Pendencias apos esta sessao

- Finalizar renomeacao `Maestro` -> `Maestro` quando o Dr. Jesus
  aprovar o estado atual.
- Popular docs oficiais OpenClaw em `docs/openclaw-official/`.
- Rodar `openclaw doctor`, `status`, `health`, `memory index` na primeira
  rodada de validacao do OpenClaw.
- Escolher pasta Python base exata para `conversation_ingestion/`.
- Relatorios iniciais sobre modelo (Opus 4.7 via API), custo, DB (Supabase
  vs alternativas), Telegram, dashboard.
- Dashboard `stemmia.com.br`: definir rotas e reaproveitar design do Planner
  Stemmia.
- Projeto paralelo de banco de laudos periciais: ainda e um eixo aberto e
  precisa de roadmap proprio depois do Maestro estar de pe.

## TODO/RESEARCH (nao mencionado ou nao fechado)

- Modelos e custos reais: nao mencionados na conversa — TODO/RESEARCH.
- Escolha definitiva do DB: nao fechada — TODO/RESEARCH.
- Estrutura real do bot Stemmia no Telegram (stack, hospedagem): nao
  mencionado na conversa — TODO/RESEARCH.
- Como o Maestro se integra com o projeto GSD ja existente no repo: nao
  mencionado na conversa — TODO/RESEARCH.

## Contexto para proxima sessao

Abrir, na ordem:
1. `MEMORY.md`
2. `memory/2026-04-22.md` (este arquivo)
3. `reports/conversation_master_summary.md`
4. `reports/conversation_next_actions.md`
5. `TASKS_NOW.md`

Proxima acao minima: validar que os 7 arquivos de sintese existem e estao
coerentes; decidir se o projeto ja pode ser renomeado para `Maestro`.
